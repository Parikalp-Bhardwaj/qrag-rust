# gRPC

gRPC is a high-performance RPC framework.
It uses Protocol Buffers to define services and messages.

In Rust, tonic is commonly used to build gRPC servers and clients.
gRPC is useful for microservices because it provides strongly typed contracts.

